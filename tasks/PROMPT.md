Implement `tasks/<TASK-FILE>.md`.

Follow `AGENTS.md` and `docs/WORKFLOW.md`.

Read the complete task specification and inspect the live repository before editing. Verify that the accepted task specification is committed. If repository evidence materially conflicts with the task and prevents safe implementation, stop and report the conflict. Otherwise, present a concise plan and proceed without waiting for further approval.

Implement only the task scope, run all required verification, update the required documentation from verified facts, and update the pull-request description and exact pushed review head as required by the task.

Create commits, push, or update a pull request only when the active task or user explicitly authorizes the exact branch and boundary. Never merge or push directly to the default branch without separate explicit authorization.
